// Loaded async by the shell's LazyMotion. domMax, not domAnimation: the
// topbar crumbs use layout animations, which the smaller bundle can't drive.
import { domMax } from "motion/react";
export default domMax;
